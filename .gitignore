/target
**/*.rs.bk
Cargo.lock.bak

.DS_Store

.idea/
.vscode/
*.swp
*.swo

/tmp/
/dist/
/build/

node_modules/
dist/
*.tsbuildinfo

# Root-only npm files: tooling installs (e.g. `vercel` CLI) drop these
# at the repo root. Workspace-level apps/*/package.json are tracked;
# these root patterns are anchored with a leading slash so they don't
# match nested package.json files.
/package.json
/package-lock.json
